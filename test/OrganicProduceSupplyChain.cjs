// const { expect } = require("chai");
// const { ethers } = require("hardhat");

// describe("Organic Produce Contract", function () {
  // it("Deployment should create produce with 0 bananas", async function () {

  //   const produce = await ethers.deployContract("OrganicProduceSupplyChain");

  //   expect(await produce.amount()).to.equal(0);

  // });

  // it("increment() should increase amount by one", async function () {

  //   const produce = await ethers.deployContract("OrganicProduceSupplyChain");
  //   await produce.increment();

  //   expect(await produce.amount()).to.equal(1);

  // });

  // it("Deploment should create bananas", async function () {

  //   const produce = await ethers.deployContract("OrganicProduceSupplyChain");

  //   expect(await produce.produce()).to.equal("Banana");

  // });

// });